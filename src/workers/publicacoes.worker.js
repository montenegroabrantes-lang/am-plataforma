// Sync de publicações desativado no Railway — Comunica API bloqueia IPs de nuvem (CloudFront 403).
// Use o script local: node ~/sync-publicacoes.mjs
export function criarPublicacoesWorker() { return null; }
